{
  "name": "phishguard-js",
  "version": "1.0.0",
  "description": "Zero-Trust Web Dependency Security Interception Framework",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npm run install:all",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others -n \"AGENT-HUB,SOC-DASH\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend"
  },
  "keywords": [
    "zero-trust",
    "security-agent",
    "dependency-shield",
    "telemetry",
    "websockets"
  ],
  "author": "PhishGuard Dev Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
